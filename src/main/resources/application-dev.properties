spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://academicis.fun:30026/transitnet?characterEncoding=UTF-8&serverTimezone=GMT
spring.datasource.username=transitnet
spring.datasource.password=transitnet

# route base uri path
transitnet.realtime.baseuri=api/
# should the data insert into database
transitnet.storage.enable=false
transitnet.index.enable=false

scheduled.historicalIndex = 0/20 * * * * ? 

mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

